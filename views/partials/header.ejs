<nav class="flex items-center bg-black text-white py-2 ">
    <div class="ml-3 relative mr-auto">
        <div class="flex h-full items-center">
            <a href="/users/profile">
                <div>
                    <img src="data:image/jpeg;base64,<%= user.dp.toString('base64') %>" 
                    <% if(page === "profile") {%>
                        class="h-10 w-10 object-cover rounded-full overflow-hidden border-2 border-white">
                        <% }else{ %>
                        class="h-10 w-10 object-cover rounded-full overflow-hidden border-zinc-500 hover:border-white hover:border-2">
                    <% } %>
                </div>
            </a>
        </div>
    </div>
    <div class="mr-6">
        <% if(page === "home") {%>
            <a href="/"><i class="ph-fill ph-house text-4xl text-white"></i></a>
        <% }else{ %>
            <a href="/"><i class="ph ph-house text-4xl text-zinc-500 hover:text-white"></i></a>
        <% } %>
    </div>
    <div class="mr-6">
    <% if(page === "post") {%>
            <a href="/posts/post"><i class="ph-fill ph-plus-circle text-4xl text-white"></i></a>
    <% }else{ %>
            <a href="/posts/post"><i class="ph ph-plus-circle text-4xl text-zinc-500 hover:text-white"></i></a>
    <% } %> 
    </div>
    
    <div class="mr-3">
        <a href="/users/logout"><button class="px-3 py-2 text-sm rounded-md bg-red-500/50 hover:bg-red-500">Logout</button></a>
    </div>
    
</nav>