<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Post</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-between">
            <h5 class="mb-5 text-2xl">Edit Post <%= //user.name %></h5>
            <a href="/logout"><button class="px-3 py-2 text-sm rounded-md bg-red-500">Logout</button></a>
        </div>
        <form action="/posts/edit/<%= post._id %> " method="post">
            <textarea class="px-3 py-2 w-96 h-28 outline-none rounded-md resize-none bg-zinc-700 block mb-3" name="content"><%= post.content %>
            </textarea>
            <label class="text-zinc-400">Edit Image</label><br>
            <input type="file" name="image"
            class="mb-5 px-2 py-2 w-72 file:h-8 file:bg-zinc-800 file:border-none file:text-white file:rounded-md rounded-lg bg-zinc-700"><br>

            <input class="px-3 py-1 bg-orange-500 rounded-md block mb-10 outline-none" value="Update?" type="submit">
        </form>        
    </div>
</body>
</html>