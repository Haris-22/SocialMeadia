<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <title>Post</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex justify-center items-center">
        <div class="h-auto w-1/3 px-auto">
            <div class="flex justify-center items-center w-full mb-10">
                <h3 class="text-4xl mb-5 font-bold">Login Your Account</h3>
            </div>
            <% if(logError.length > 0) {%>
            <div class="w-full mb-5 flex justify-center items-center">
                <h1 class="bg-red-600 w-72 px-3 py-1 flex items-center"><i class="ph-bold ph-warning-circle mr-1"></i><%= logError %></h1>
            </div>
            <% } %>
            <% if(error.length > 0) {%>
                <div class="w-full mb-5 flex justify-center items-center">
                    <h1 class="bg-red-600 w-72 px-3 py-1 flex items-center"><i class="ph-bold ph-warning-circle mr-1"></i><%= error %></h1>
                </div>
                <% } %>
            <div class="flex justify-center items-center w-full mb-10">
                <form action="/login" method="post">
                    <input type="email" name="email" placeholder="Email" class="h-10 w-72 bg-zinc-700 px-3 py-1 rounded-md mb-5 outline-none"><br>
                    <div class="relative h-10 w-72">
                        <input type="password" name="password" placeholder="Password" 
                        class="password h-full w-full bg-zinc-700 px-3 py-1 rounded-md mb-7 outline-none">
                        <span class="eye-icon absolute top-1/2 right-2 transform -translate-x-1/2 -translate-y-1/2 text-zinc-400" 
                        onclick="togglePassword()"><i class="ph ph-eye-closed"></i></span>
                    </div><br>
                    <input type="submit" name="login" class="bg-violet-600 px-3 py-1 rounded-full w-full outline-violet-600">
                </form>
            </div>
            <div class="flex justify-center items-center w-full">
                <h3 class='mt-5 text-zinc-500'>Don't have any account <a href="/register" class="text-violet-600 underline ">SignIn.</a></h3>
            </div>
        </div>
    </div>

    <script>
        const password = document.querySelector('.password');
        const eyeIcon = document.querySelector('.eye-icon');
        
        
function togglePassword() {
    if (password.type === 'password') {
        password.type = 'text';
        eyeIcon.innerHTML='<i class="ph ph-eye"></i>'
    } else {
        password.type = 'password';
        eyeIcon.innerHTML='<i class="ph ph-eye-closed"></i>'
    }
}

    </script>
</body>
</html>